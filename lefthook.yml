pre-commit:
  parallel: true
  commands:
    lint:
      run: just lint
    format:
      run: just format-check
    check:
      run: just typecheck
    typos:
      run: just typos

pre-push:
  commands:
    build:
      run: just build
